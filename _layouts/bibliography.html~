---
---

{{reference}}

<style>
  .vspace20px {
     margin-bottom: 20px;
  }
</style>

<div id="{{key}}-materials">
	
  <div class="nav nav-pills">
    {% if entry.abstract %}
    <a data-toggle="collapse" href="#{{key}}-abstract">Abstract</a>
    {% endif %}

    {% if entry.paperbibtex %}
    <a href="{{entry.paperbibtex}}">[bib]</a>&nbsp;
    {% endif %}

    {% if entry.paperpdf %}
    <a href="{{ site.url }}/pdf/{{entry.paperpdf}}">[PDF]</a>&nbsp;
    {% endif %}
	
		{% if entry.abstract %}
		<p id="{{key}}-abstract" class=".accordion-group">{{entry.abstract}}</p>
		{% endif %}
	
		 <button data-toggle="accordion" data-target="#demo">Collapsible</button>

		<div id="demo" class="collapse">
		Lorem ipsum dolor text....
		</div>


  </div>
	<p class="vspace20px">
	</p>
	<!--
  <pre id="{{key}}-bibtex" class="pre pre-scrollable collapse">{{entry.bibtex}}</pre>
	-->
</div>

